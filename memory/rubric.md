<assignment_question>
Write a Python function `is_palindrome(text)` that takes a string and returns True if it is a palindrome, and False otherwise. It must ignore spaces and be case-insensitive.
</assignment_question>

<rubric>
- 10/10: Perfect implementation handling spaces and case insensitivity (e.g., "Race car" -> True).
- 7/10: Logic is mostly correct, but fails on edge cases (e.g., forgets to lowercase the string or strip spaces).
- 4/10: Major logical flaws but valid Python syntax.
- 0/10: Does not run due to syntax errors.
</rubric>